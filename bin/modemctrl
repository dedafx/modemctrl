#!/usr/bin/env python3

import sys
from dedafx import modemctrl

def main(force=False):
    if modemctrl.isConnectionSlow():
        modemctrl.reboot()

if __name__ == '__main__':
    force=False
    if '--force' in sys.argv:
        force = True
    main(force=force)